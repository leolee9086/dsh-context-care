# 待办：SillyTavern 那套正则脚本（2026-09-21）

## 目标

把**正则脚本**这件事落到 `dsh-context-care`：一套"对喂给模型的文本做正则改写"的机制，
由用户定义规则（查找 / 替换 / 作用范围），在拼上下文的时候应用。

## 为什么落在 context-care 而不是别处

它做的事情是**对上下文的文本变换** —— 改写即将进入模型的那段文字。
这正是 context-care 管的事（它已经在管压缩、保留、替换）。放在别处会变成
"另一个插件也在改上下文"，两处都能改同一样东西，迟早打架。

## 它从哪来：角色卡里带得动

SillyTavern 生态里的角色卡可以**自带正则脚本**（社区插件 `dsh-muv-table` 的介绍里就写着
「支持直接读取 SillyTavern PNG 角色卡（**含正则脚本**、世界书、变量表）」）。

马杜克读角色卡时（`dsh-marduk` 的 `src/questionnaire-chara.js`），会把卡里的 `extensions`
那一整块**原样存成一条作答**（JSON 文本）—— 也就是说正则脚本的**数据进得来、存得下**，
但马杜克**既不解析它、也不应用它**。解析和应用都是 context-care 的活儿。

## 交接方式：事件 + 文件数据协议，**不互相声明依赖**

按哥定的规矩：插件之间能靠**事件 + 文件数据协议**交换数据，就**不互相声明依赖**；
互相调用**永远以消息范式优先**。

所以这里的形状应该是：

```
dsh-marduk  读到卡 → 把正则脚本以事件(或落盘文件)的形式交出去
                                  ↓
dsh-context-care  订阅/读取 → 应用规则
```

**两边谁都不 import 谁。** 马杜克不装 context-care 也能导入卡（只是那些正则没人应用）；
context-care 不装马杜克也能用（只是没有卡来源的规则）。

具体用哪个事件、数据落成什么文件 —— **还没定，也还没查**。

## 结构:查证结果(2026-09-21,读的是酒馆本体)

**来源**:`SillyTavern/SillyTavern` 的 `public/scripts/extensions/regex/engine.js`(AGPL-3.0)。
这是**行为准绳** —— 按哥定的规矩,参考酒馆本体,不去引 npm 上的二手描述。

### 存在哪

角色卡里的 **`extensions.regex_scripts`**(数组)。
酒馆自己还带一批**内建脚本**(仓库里有 `embeddedScripts.html` / `presetEmbeddedScripts.html`)。

### 一条规则长什么样

| 字段 | 作用 |
|---|---|
| `scriptName` | 名字 |
| `findRegex` | 查找(正则串) |
| `replaceString` | 替换;支持 `{{match}}`(等价 `$0`)和 `$1` / `$<名>` 分组 |
| `trimStrings` | **替换结果里要再去掉的字符串数组** |
| `substituteRegex` | 0 / 1 / 2 —— 是否先对 `findRegex` 做宏替换(0 原样、1 替换、2 替换+清洗) |
| `placement` | **作用位置数组**,值是 `regex_placement` 枚举 |
| `disabled` | 开关 |
| `markdownOnly` | 只作用于 markdown 输出 |
| `promptOnly` | 只作用于提示词 |
| `runOnEdit` | 编辑消息时也跑 |
| `minDepth` / `maxDepth` | **作用深度范围**(聊天历史里多深) |

### 它是怎么被调用的(这一条决定我们的形状)

入口是 `getRegexedString(rawString, placement, { characterOverride, isMarkdown, isPrompt, isEdit, depth })`。

也就是说:**引擎是被"位置 + 上下文"驱动的**,一条规则自己声明它管哪些位置
(`script.placement.includes(placement)`),调用方按位置把字符串送进来。
不是"遍历所有规则去改一个全局文本"。

### 对应到那三个待查问题

1. **确切结构** —— ✅ 见上表
2. **作用范围** —— ✅ **按 placement 分**:规则自己声明管哪些位置;
   再叠加 `minDepth`/`maxDepth`(深度)、`markdownOnly`/`promptOnly`(输出去向)两层过滤
3. **和已有压缩逻辑的先后** —— ❌ **还没定**。这是 context-care 自己的设计问题:
   正则在压缩**前**跑(压缩看到的是改写后的文本)还是**后**跑(压缩看到原文、只在送模型前改写)?
   两种都有道理,要先想清楚再动手。

## 状态

**只记一笔,还没实现。**
