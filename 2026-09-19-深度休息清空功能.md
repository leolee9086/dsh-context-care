# 深度休息：清空式压缩（2026-09-19）

## 目标

给 `context_rest` 加一条清空路径：不调 LLM 生成摘要，直接拿模型自己写的交接当替换物，
把历史清掉。这样就不会撞上 DSH 本体那条死路 ——
`compaction-basic` 的 `region.ts:419` 要求摘要严格小于被压区间，而摘要有个约 3200 token
的下限（框架 + 模型输出），区间一旦缩到同量级，压缩就永久失败，重试也没用。

## 背景（为什么是清空而不是更好的摘要）

- 触发侧用 provider 实测（那个会话 63.8 万，含 cacheRead），选择侧用 token meter 估算
  （13.1 万，图片按 50~60 token/张计价而实际约 1400）——两个口径分叉，retain 预算
  （窗口 × 0.16）几乎吃光 meter 算出的 surface，可压区间只剩 3168。
- 就算口径对齐，**替换式压缩反复执行必然收敛到失败**：对摘要再摘要不会更短。
- S-forge 的"深度休息"走的是另一条路：清空 + 只回填 system prompt，先落盘再清空。
  它的围栏是概率和信任（三贤者疲劳曲线不同步、唤醒值提醒模型去找回），不是硬约束。
- Codex 最新的 TokenBudget 模式也是这个方向（`start_new_context_window` + history/notes 工具），
  但它只在 openai provider + codex backend 下启用，中转站用不了。

## 改了什么

**`src/selection.js`** — 新增 `selectClearRange(session)`
- 与 `selectRestRange` 的区别：不保留尾部、不要 `minFreshTokens` 门槛（清空的替换物永远比区间小）。
- 保留 tool 批次边界规则（切口必须落在所有工具调用都被应答处）。
- **从 surface node 1 起算**（node 0 是系统提示词）。`assertSystemHeadRewrite`
  （`core/session/src/surface.ts:444`）会拒绝任何 `startIdx === 0` 且该节点是
  `system/message` 的替换 —— 清空历史不能连规则一起清掉。

**`src/deep-rest.js`** — 新增（整份新文件）
- `inspectSession(session)`：从日志推出 openTurn 和未闭合的 compaction，纯读取。
- `clearRange(session, meter, range, text)`：写完整事务
  `compaction/start` → `compaction/summary` → `user/message`(replace) → `compaction/end`。
  四处 compactionId 相同，`shadowedSeqs` 精确列出区间内每个节点，owner 取推出来的 openTurn。
  取区间、测量、写事件之间不 await（中间让出的话 surface 会变，`shadowedSeqs` 就对不上了）。
  失败时也写一条带 error 的 `compaction/end`，否则未匹配的 start 会永久挡住这个会话的后续压缩。
- **不 import 任何 `@deepseek-ai/*`**：`session.append` + `surfaceOp` 是 session 的公开契约
  （`core/session/src/types.ts:437` 原文写着 "any surface-replacing producer may use it"），
  checkpoint 的 source 标记 `{kind:'plugin', plugin:'compact', compactionId}` 是固定字符串。

**`src/index.js`**
- `notice()` 加 `extra` 参数（结构化标记）。
- `context_rest` 加两个参数：`deep`（布尔）和 `recovery`（字符串）。
  `deep: true` 时 `recovery` 必须非空 —— 清空之后那段文字是唯一的回程。
  未知参数单独报错（和「note 不合法」分开，对调用方是两种不同的修法）。
- `agent/pre-step` 按 `deep` 分支：清空路径**不经过 compaction provider**，
  直接 `selectClearRange` + `clearRange`。

## recovery 由模型填，插件只要求它存在

通用指引（"用 session_blocks_search 搜关键词"）对每个会话都一样，等于零信息。
模型自己写的才是具体的：搜什么词、看哪个文件、去哪找。
所以插件做三件事：要求它必须存在、把它拼进替换物、加框架文字说明这是什么。

注入的消息文本 = 交接 + 找回路径两段，清空后这条消息整体成为替换物。

## 证据

```
node --check src/{deep-rest,selection,index,host}.js   # 通过
npm test                                              # 49 pass / 0 fail（原 46 + 新增 3）
```

新增的三个测试：
1. `clear range keeps the system prompt at node 0 and stops on a balanced tool edge`
2. `clear range declines when only the system prompt would be left`
3. `deep rest clears the history to the handoff and never calls the compaction provider`
   （断言 `h.compacted.length === 0` —— 证明清空没走 provider）

测试 harness 的 `history()` 补了 `append` / `snapshotEvents`（纯加法），
`append` 会像真实 session 那样维护 surface，这样清空后的结果能从 `surface.nodes` 观察到。

## 待办

- **生效需要重启**：`src/` 直接是 exports 入口，改了要重启 harness（HMR 到不了）。
- **版本号**：`package.json` 还是 0.5.0，加了功能应该 bump（`private: true`，不发布，只是本地标记）。
- **`renderState` 分级：已完成（2026-09-19 稍晚）。** 四级分级 + 模型面向提示全部中文化，
  详见 memos 里的 `2026-09-19-上下文提示中文化与分级.md`。原来的问题描述保留如下：
  疲劳度不管哪一级都输出同一句
  `Continue the task; use context_rest when a checkpoint would help.`，
  而 GUIDANCE 里那句 `Do not rush, stop, or repeatedly compact solely because of a status label`
  不分等级，在 very-high 时构成反向抑制。四级草案已给哥哥（normal/elevated/high/very-high，
  very-high 明确指向清空硬切）。
- **真机验证：已完成。** 2026-09-19 稍晚，本插件所在的开发会话自己跑了一次 `deep: true`：
  263874 tokens 的历史被清成「系统提示词 + 交接 + 找回路径」，compaction provider 零调用。
  清空功能本身没问题。

## 关键路径

- 插件：`D:\dev\dsh-context-care`（`src/index.js`、`src/selection.js`、`src/deep-rest.js`、`src/policy.js`）
- 测试：`test/context-care.test.js`
- DSH 契约：`D:\dev\deepseek-harness\packages\core\session\src\{types.ts,surface.ts,index.ts}`
- 摘要路径的判定：`D:\dev\deepseek-harness\packages\compaction\compaction-basic\src\region.ts:419`
- 完整诊断笔记：`C:\Users\al765\.dsh\.agent-presets\cordis-no-subagents\skills\memos\2026-09-19-DSH上下文压缩死锁与深度休息.md`
